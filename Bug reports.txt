1# У лого taulia в верхнем левом углу страницы отсутствует ссылка для перехода на главную страницу

2# "Search my invoices": только у названия поля "From amount:" присутсвует ":", у остальных названий полей двоеточие отсутсвует

3# "Search my invoices": у иконки бинокль отсутсвует подсказка(tooltip) Search

4# Иконка дискетки у "Download list" не корректно отображает смысл названия.

5# В таблице инвоисов отображено 11 строк, а должно 10. Так как в "Search my invoices" в поле "Results per page" указано 10

6# В 11 строке 9 столбца таблицы инвоисов отсутсвует кнопка "Actions"

7# Таблица инвоисов 7 столбец "Status" 4 строка: У иконки не корректно указана подсказка(tooltip) вместо "Paid" указано "Approved"

8# Исправить название первого столбца таблицы инвоисов с "Invoce#" на "Invoice"

9# 7 столбец "Status" первая строка таблицы инвоисов: заменить "Rejcted" на "Rejected"

10# Нужен ли столбец 2 "Type" в таблице инвоисов, у которого все значения одинаковые("Invoice")?

11# Столбец 6 "Amount" в таблице инвоисов: несоответсвие выбранной сортировки от меньшего к большему в строке 7 "$22,409.37" и строке 8 "$22,376.00". Значение строк 7 и 8 необходимо поменять местами,так как $22,409.37">"$22,376.00"

12# Несоответсвие формата дат в столбце 3 "Inv.Date" таблицы инвоисов (Например:"5-Jun-2011") и формата дат в "Search my invoices" поле "Invoice date" (Например: "6/5/2011")

13# Значения в поле "Due Data" таблицы инвоисов не соответсвуют названию данного поля

14№ Значения в поле "PO #" таблицы инвоисов не соответсвуют названию данного поля

15# "Showwing 422 invoices": значение 422 не соотвествует списку ссылок на страницы внизу таблицы инвоисов "1 2 3 4 6 7 8 9 10 .. 32" (итого получается 320 инвоисов)

16# Внизу таблицы инвоисов в списке ссылок на страницы "1 2 3 4 6 7 8 9 10 .. 32" отсутсвует значение 5

17# При наведении курсора мыши на кнопки "Actions" в 9 столбце таблицы инвоисов курсор стрелка не меняется на пальчик

18# "Search my invoices": в названии "From amount:" перед "text field" добавить знак "$"

19# Значение "1-Oct-2011" в столбце "Inv. Date" строка 7 таблицы инвоисов не соответсвует диапазону значений в "Search my invoices"  поле "Invoice date"  from "6/5/2011" to "9/30/2011"   